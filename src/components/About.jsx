import React from "react";
import {Tilt} from "react-tilt";
import { motion } from "framer-motion";

import { styles } from "../styles";
import { services } from "../constants";
import { SectionWrapper } from "../hoc";
import { fadeIn, textVariant } from "../utils/motion";

const ServiceCard = ({ index, title, icon }) => (
  <Tilt className='xs:w-[250px] w-full'>
    <motion.div
      variants={fadeIn("right", "spring", index * 0.5, 0.75)}
      className='w-full green-pink-gradient p-[1px] rounded-[20px] shadow-card'
    >
      <div
        options={{
          max: 45,
          scale: 1,
          speed: 450,
        }}
        className='bg-tertiary rounded-[20px] py-5 px-12 min-h-[280px] flex justify-evenly items-center flex-col'
      >
        <img
          src={icon}
          alt='web-development'
          className='w-16 h-16 object-contain'
        />

        <h3 className='text-white text-[20px] font-bold text-center'>
          {title}
        </h3>
      </div>
    </motion.div>
  </Tilt>
);

const About = () => {
  return (
    <>
      <motion.div variants={textVariant()}>
        <p className={styles.sectionSubText}>Introduction</p>
        <h2 className={styles.sectionHeadText}>About Me</h2>
      </motion.div>

      <motion.p
        variants={fadeIn("", "", 0.1, 1)}
        className='mt-10 text-secondary text-[19px] text-justify max-w-10xl leading-[30px]'
      >
       I am a senior undergraduate student majoring in Computer Science at the
        University of Texas at Arlington. An accomplished research assistant
        with hands-on experience in web development at CSE UTA-IT lab UTA and a
        commitment to continuous learning, I am confident in my ability to drive
        results and contribute to the success of any organization. My major
        passion lies in Web development, Mobile App development, and Software
        Development on both agile and waterfall methods. Currently, I am seeking
        a full-time opportunity to further develop my skills and make a positive
        impact in the industry. After graduation in Dec 2023, I am eager to
        leverage my technical and leadership abilities in a challenging work
        environment. Feel free to contact me at samrat.baral@mavs.uta.edu
      </motion.p>

      <div className='mt-20 flex flex-wrap gap-10'>
        {services.map((service, index) => (
          <ServiceCard key={service.title} index={index} {...service} />
        ))}
      </div>
    </>
  );
};

export default SectionWrapper(About, "about");
